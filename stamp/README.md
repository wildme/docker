# Shell variables
Following variables must be defined on the host to run Stamp in Docker:  

**DOCKER_VOL_STAMP_FILES** *// path to the attachments storage*  
**DOCKER_VOL_STAMP_LOGS** *// path to the logs storage*  
**DOCKER_VOL_STAMP_DB** *// path to the DB storage*  
